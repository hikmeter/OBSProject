@model List<ResultFacultyDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    int count = 0;
}

<div class="page-content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-xl-12">
                <div class="card shadow-sm border-0">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <div>
                                <h4 class="mb-1">🎓 Fakülte Listesi</h4>
                                <p class="text-muted mb-0"> üniversitenizde bulunan fakülteleri buradan yönetebilirsiniz.</p>
                            </div>
                            <a href="/AdminFaculty/CreateFaculty" class="btn btn-primary">
                                + Yeni Fakülte Ekle
                            </a>
                        </div>
                        @if (Model != null && Model.Count > 0)
                        {
                            <div class="table-responsive">
                                <table class="table table-striped table-bordered align-middle">
                                    <thead class="table-light">
                                        <tr>
                                            <th style="width: 50px;">#</th>
                                            <th>Fakülte Adı</th>
                                            <th style="width: 100px;">İşlemler</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var item in Model)
                                        {
                                            count++;
                                            <tr>
                                                <td>@count</td>
                                                <td>@item.facultyName</td>
                                                <td>
                                                    <div class="d-flex gap-2">
                                                        <a href="/AdminFaculty/UpdateFaculty/@item.facultyID" class="btn btn-sm btn-outline-success" title="Güncelle">
                                                            <i class="bi bi-pencil-square"></i>
                                                        </a>
                                                        <a href="/AdminFaculty/RemoveFaculty/@item.facultyID" class="btn btn-sm btn-outline-danger" title="Sil" onclick="return confirm('Bu fakülteyi silmek istediğinize emin misiniz?');">
                                                            <i class="bi bi-trash"></i>
                                                        </a>
                                                    </div>
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        }
                        else
                        {
                            <div class="alert alert-info mt-3">
                                Sistemde tanımlı fakülte bulunmamaktadır.
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>